{
  "name": "cyrus",
  "version": "1.1.1",
  "description": "Automated trades and transfers for Kraken exchange",
  "main": "dist/main.js",
  "scripts": {
    "start": "npm run build && electron .",
    "build": "tsc",
    "watch": "tsc -w",
    "dev": "npm run build && electron . --dev",
    "pack": "electron-builder --dir",
    "dist": "electron-builder",
    "build:backend": "cd src/backend && call venv\\Scripts\\activate && pyinstaller server.spec",
    "build:all": "npm run build && npm run build:backend && npm run dist"
  },
  "build": {
    "appId": "com.cyrus.app",
    "productName": "Cyrus",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "src/index.html",
      "src/donation.js",
      "src/app/**/*",
      "src/assets/**/*",
      "!src/**/*.ts",
      "!src/backend/**/*"
    ],
    "extraResources": [
      {
        "from": "src/backend/dist/CyrusServer.exe",
        "to": "backend/CyrusServer.exe"
      }
    ],
    "win": {
      "icon": "src/assets/icon.ico",
      "signAndEditExecutable": false,
      "target": [
        "nsis"
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "allowElevation": true,
      "perMachine": false,
      "deleteAppDataOnUninstall": false,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    }
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "electron": "^28.2.0",
    "electron-builder": "^24.13.3",
    "typescript": "^5.3.3"
  }
}
